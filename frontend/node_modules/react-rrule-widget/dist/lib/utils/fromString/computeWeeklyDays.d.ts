import { Options } from "rrule";
import { Model } from "../Model";
declare const computeWeeklyDays: (data: Model, rruleObj: Partial<Options>) => {
    mon: boolean;
    tue: boolean;
    wed: boolean;
    thu: boolean;
    fri: boolean;
    sat: boolean;
    sun: boolean;
};
export default computeWeeklyDays;
