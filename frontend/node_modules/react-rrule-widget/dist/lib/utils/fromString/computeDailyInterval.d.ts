import { Options } from "rrule";
import { Model } from "../Model";
declare const computeDailyInterval: (data: Model, rruleObj: Partial<Options>) => number | undefined;
export default computeDailyInterval;
