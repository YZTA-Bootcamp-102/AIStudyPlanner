import { Locales } from '../../translations';
import { Model } from '../../utils/Model';
type Props = {
    end: Model["end"];
    handleChange: any;
    locale: Locales;
    translations: any;
    availableEndOptions: ("never" | "on-date" | "after-executions")[];
};
export default function End({ end: { mode, after, onDate }, handleChange, translations, locale, availableEndOptions, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
