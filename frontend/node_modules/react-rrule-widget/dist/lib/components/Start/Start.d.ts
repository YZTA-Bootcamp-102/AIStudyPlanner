interface IProps {
    start: {
        onDate: any;
    };
    handleChange: (p: any) => void;
    translations: any;
    locale: string;
}
export default function Start({ start: { onDate }, handleChange, translations, locale, }: IProps): import("react/jsx-runtime").JSX.Element;
export {};
